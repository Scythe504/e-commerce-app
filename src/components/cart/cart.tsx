import { CartFooter } from "./cart-footer/cart-footer"
import { CartItems } from "./cart-items/cart-item"

export const Cart = ()=>{
    return <div>
        <CartItems/>
        <CartFooter/>
    </div>
}